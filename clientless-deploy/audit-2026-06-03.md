# Live Box Audit - 2026-06-03

Target: `sdanced@139.180.144.61` (`clientless-orch`, Ubuntu kernel `5.15.0-179-generic`).
Audit was read-only; no live processes were stopped or restarted.

## Current State

| Row | Item | Observed state |
| --- | --- | --- |
| 1 | `sdanced` user | Exists as `uid=1001`, passwordless sudo works, operator pubkey is present. `loginctl` shows `Linger=no`. |
| 2 | Claude creds | `/home/sdanced/.claude/.credentials.json` exists mode `0600`; `claude --version` reports `2.1.160`. `orchestrate-lean.md` exists at `/home/sdanced/.claude/commands/orchestrate-lean.md`. |
| 3 | SpacetimeDB | `/home/sdanced/.local/bin/spacetime` resolves to SpacetimeDB `2.1.0`. Data dir is `/home/sdanced/.local/share/spacetime/data`. It listens on `127.0.0.1:3001`. |
| 4 | Harness module | DB `orchestrator-box` is reachable through the harness CLI on `http://127.0.0.1:3001`; `harness facts` returns successfully. `spacetime list` under the current CLI identity does not list it, but CLI calls against `orchestrator-box` work. |
| 5 | Harness CLI | `/home/sdanced/orchestrator/harness` exists and runs. Built from `/home/sdanced/orchestrator/harness-rs` with feature `cli`. |
| 6 | `harness.toml` | Present in `/home/sdancer/orchestrator/harness.toml`, `/home/sdanced/orchestrator/harness.toml`, and `/home/sdanced/orch-rust-dash/dashboard/harness.toml`; all point at `server = "http://127.0.0.1:3001"` and `database = "orchestrator-box"`. |
| 7 | DB-wired loop | `/home/sdanced/clientless/onbox/tick.sh` mirrors each new episode line into SpacetimeDB with `episode-add`, `goal-add`, `sub-goal-add`, and `path-add`. `loop.sh` also regenerates `paths.json` with `gt2paths.py`. |
| 8 | Rust dashboard | Binary is `/home/sdanced/orch-rust-dash/dashboard/target/release/orchestrator-dashboard`; it binds `0.0.0.0:3030` and returns `303 /login` locally. `app.py` is absent. |
| 9 | Cloudflared sg2 tunnel | `/usr/local/bin/cloudflared` version `2026.5.2` is installed. A `cloudflared tunnel run --token ...` process is running. No cloudflared systemd unit exists. |
| 10 | Lean skill | `/home/sdanced/.claude/commands/orchestrate-lean.md` exists and is refreshed from the host. |
| 11 | Pool access | `/home/sdanced/.ssh/id_ed25519` exists mode `0600`; `/home/sdanced/clientless/pool.json` exists and contains a live inventory object with `note`, `count`, and `boxes`. |
| 12 | Two trees | Both `/home/sdancer/orchestrator` and `/home/sdanced/orchestrator` exist on the box. The live loop and dashboard use `/home/sdanced`; `/home/sdancer` is a compatibility mirror containing matching harness artifacts. |
| 13 | Knowledge base | `/home/sdanced/albion-wiki/WIKI.md`, `/home/sdanced/albion/tools/input/navi-e2e/albion_e2e.py`, and `/home/sdanced/albion/tools/input/albion_ingame_register_login.py` exist. Bootstrap now mirrors host `/home/sdancer/albion-wiki/` and `/home/sdancer/albion/tools/`. |

## Keepalive

| Service | Current keepalive | Reboot durable today? | Bootstrap target |
| --- | --- | --- | --- |
| SpacetimeDB | Root systemd unit `/etc/systemd/system/spacetimedb-box.service`, enabled and active. | Yes. | Keep as root unit, install idempotently. |
| Rust dashboard | Detached process with parent PID 1, launched from `/home/sdanced` with the dashboard binary. | No. | Install root systemd unit with `User=sdanced` and `WorkingDirectory=/home/sdanced/orch-rust-dash/dashboard`. |
| Cloudflared | Detached `cloudflared tunnel run --token ...` process with parent PID 1. | No. | Install root systemd unit using `/etc/clientless-orchestrator/cloudflared.env`. |
| Lean loop | Detached `bash /home/sdanced/clientless/onbox/loop.sh` process with parent PID 1. | No. | Install root systemd unit with `User=sdanced` and `WorkingDirectory=/home/sdanced/clientless`. |
| `gt2paths.py` | User crontab `*/2 * * * * python3 /home/sdanced/clientless/onbox/gt2paths.py`. | Yes while cron is active. | Keep crontab for compatibility; loop service also calls it after each tick. |

## Bootstrap Notes

The live dashboard, cloudflared tunnel, and loop are unmanaged processes. A safe live bootstrap must install and enable their systemd units without starting duplicate services while those unmanaged processes already occupy the role or port. After the next reboot, the enabled systemd units should own them.

## Hardened Bootstrap Validation

Ran `./bootstrap.sh 139.180.144.61 22` twice on 2026-06-03. Both runs completed without stopping the live unmanaged dashboard, tunnel, or loop. The second run was idempotent: systemd units stayed enabled, existing unmanaged processes were not duplicated, DB `127.0.0.1:3001` and dashboard `0.0.0.0:3030` each had one listener, `https://sg2.orch.run/` returned `303 /login`, and the projection crontab remained:

```cron
*/2 * * * * python3 /home/sdanced/clientless/onbox/gt2paths.py >/dev/null 2>&1
```
